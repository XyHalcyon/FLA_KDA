# chunk_kda 算子优化总览

> 测试入口：`tests/kernels/test_chunk_kda_npu.py::test_chunk_kda`
> Profiling 数据：`/home/xxx/profiling/total/`（8K case）与 `/home/xxx/profiling/total2/`（1K case）
> 优化分支：`xy_glm5.1`，基线：`main`（commit `bdcc686`）

## 一、整体效果

| 维度 | 8K case (cu=[0,8192]) | 1K case (cu=[0,1024]) |
|---|---:|---:|
| chunk_kda 流水合计（仅 fla/triton kernel） | 110,053 → **20,383 us（-81.5%）** | 13,933 → **2,779 us（-80.1%）** |
| 流水节省时间 | 89,670 us | 11,154 us |
| device 总耗时（含 DSARandom 等数据生成） | 610,069 → 569,431 us（-6.7%） | 80,386 → 70,030 us（-12.9%） |
| 测试输入 | B=1, T=8192, H=32, D=128, fp16 | B=1, T=1024, H=32, D=128, fp16 |
| 算法正确性 | `o rmse=0.000000`，`ht rmse=0.000314`，远低于阈值 0.005 | 同上 |

两个 case 的瓶颈分布、各 kernel 的相对收益百分比几乎完全一致，证明优化效果对序列长度高度鲁棒。

## 二、各 kernel 优化点与收益

### 1. `chunk_local_cumsum_vector_kernel`（cumsum.py）

**作用**：对 gate 张量按 chunk 做累积和，输出 `gk = cumsum(g)` 在 fp32 中。

**优化点**（提交 `6a46517`、`e990d3d`、`c11b3f7`）：
1. **用 `tl.cumsum` 原生算子替代 `tl.dot(下三角 mask, x)`**：原实现用 `tl.where(o_i[:,None]>=o_i[None,:], 1.0, 0.0)` 构造 64×64 下三角矩阵后跟 `tl.dot`，scalar 占比 96.9%。改为 `b_o = tl.cumsum(b_s, axis=0)`，原生算子直接在 vec 单元上扫描。
2. **BS=32 → 64**：vec 列分块加倍，Block Dim 16384 → 8192，wave 数减半。
3. **H_PACK=8**：一个 program 静态展开 8 个 head，Block Dim 8192 → 1024，wave 数 ~205 → ~26。

**关键指标**：

| 指标 | 8K BASE | 8K NEW | 1K BASE | 1K NEW |
|---|---:|---:|---:|---:|
| Task Duration | 52,583 us | **398 us** | 6,606 us | **54 us** |
| 收益 | - | **-99.2%** | - | **-99.2%** |
| Block Dim | 16,384 | 1,024 | - | - |
| aiv_scalar_ratio | 96.8% | 26.2% | - | - |

---

### 2. `chunk_gla_fwd_kernel_o`（kda.py）

**作用**：把 chunk 间项 `q·exp(gk)·hᵀ` 与 chunk 内项 `Aqk·v_new` 相加得到最终输出 `o`。

**优化点**（提交 `8fa28de`、`3837b60`、`5ee505f`、`2da40ce`、`6b0a5de`）：
1. **预计算 tril mask**：把 kernel 内 `tl.arange(0,BT)[:,None]>=tl.arange(0,BT)[None,:]` 构造的下三角 mask 改成 caller 端 `torch.tril(...)` 预算好直接传 buffer。AIV scalar 占比 95.6%→62.2%。
2. **预计算 qg 和预转置 h**：把 `q*scale*exp(g)` 与 `h.transpose(-1,-2)` 提到 Python 端。
3. **autotune 收敛**：从 24 组 sweep 配置精简到单点 `BK=128, BV=128, num_warps=4`，干掉 i_k/i_v 控制流。
4. **预计算 `A*tril_mask`** 让 kernel 直接 dot 不带 mask。
5. **H_PACK=8**：grid 第三维 `B*H=32` 降到 `B*(H/H_PACK)=4`，wave 数 ~171 → ~22。

**关键指标**：

| 指标 | 8K BASE | 8K NEW | 1K BASE | 1K NEW |
|---|---:|---:|---:|---:|
| Task Duration | 13,673 us | **521 us** | 1,724 us | **57 us** |
| 收益 | - | **-96.2%** | - | **-96.7%** |
| Block Dim | 4,096 | 512 | - | - |
| aic_mac_ratio | 0.5% | 11.4% | - | - |

---

### 3. `chunk_kda_scaled_dot_kkt_fwd_kernel_intra_sub_inter`（kda.py）

**作用**：算 `β·K·Kᵀ` 和 `q·Kᵀ` 的 chunk 内非对角块（i_i > i_j）。

**优化点**（提交 `7ecae2f`、`b632097`、`e9281bb`）：
1. **`BK = next_power_of_2(K)` + `tl.static_assert(BK >= K)`**：让 K 维 i_k 循环塌缩为单步。
2. **i_i-only 载入 hoist 到 i_j 循环外**：原代码每轮 (i_i, i_j) 都重新 load `q/k/g` 与 `b_gn`，hoist 后 NC=4 时 i_j 循环只读一次 q/k/g 和算一次 `exp(b_g - b_gn)`。
3. **β 预折进 b_kg + 去掉累加器**：`b_A = tl.zeros + tl.dot + b_A * b_b` 改成 `b_kg = b_k * exp(b_g) * β`，再 `b_A = tl.dot(b_kg, b_ktg)`。省一次 BC×BC vec pass、一对 `tl.zeros` 初始化。
4. **`tl.static_range` 静态展开 i_i / i_j 三角循环**：偏移变编译期立即数，aic_scalar 从 13.5us 降至 1.0us（-92.4%）。

**关键指标**：

| 指标 | 8K BASE | 8K NEW | 1K BASE | 1K NEW |
|---|---:|---:|---:|---:|
| Task Duration | 13,395 us | **6,160 us** | 1,695 us | **899 us** |
| 收益 | - | **-54.0%** | - | **-47.0%** |
| Block Dim | 4,096 | 4,096 | - | - |
| aic_scalar_ratio | 60.6% | 3.3% | - | - |
| aic_mac_ratio | 2.0% | 2.4% | - | - |

注：grid 与 BASE 相同，本 kernel 优化全部来自单 program 内部的循环重构和载入合并。

---

### 4. `chunk_kda_scaled_dot_kkt_fwd_kernel_intra_sub_intra`（kda.py）

**作用**：算 chunk 内对角块 (i_i, i_i) 的 `β·K·Kᵀ` 和 `q·Kᵀ`。

**优化点**（提交 `b9a38be`、`907a9bf`）：
1. **用 `tl.dot + 下三角 mask` 替代 BC 轮 vec sum reduce**（最关键）：
   - 原实现：`for j in range(BC)` 循环 16 轮，每轮 `tl.sum(b_k * b_ktg, axis=1)` 走 vec 单元，**整个 kernel 跑纯 AIV，cube 利用率 0%**。
   - 数学重构：`A[i,j] = sum_k b_k[i,k] * b_kt[j,k] * exp(b_g[i,k] - b_gk[j,k])` = `(b_k*exp(b_g)*β) · (b_k*exp(-b_g))^T`，整个对角块用 2 次 BC×BC dot 一次性算完，再用 `tl.where(o_i>j)`/`o_i>=j` mask 出下三角和上三角。
   - **exp 调用次数从 32K 降至 2K（-94%）**。
   - aiv_vec_time 27.1us → 1.0us（-96.3%）；aiv_mte3_wait_ratio 25.2% → 0.02%（彻底消除写出排队）。
2. **H_PACK=8**：grid 第三维从 `B*H=32` 降到 `B*(H/H_PACK)=4`，Block Dim 16384 → 2048，wave 数 ~683 → ~85。

**关键指标**：

| 指标 | 8K BASE | 8K NEW | 1K BASE | 1K NEW |
|---|---:|---:|---:|---:|
| Task Duration | 15,788 us | **4,353 us** | 2,009 us | **578 us** |
| 收益 | - | **-72.4%** | - | **-71.2%** |
| Op Type | pure vector | **mix（cube 启用）** | - | - |
| Block Dim | 16,384 | 2,048 | - | - |
| aic_mac_ratio | 0.0% | 2.3% | - | - |
| cube_utilization | 0.0% | 96.2% | - | - |

---

### 5. `merge_16x16_to_64x64_inverse_kernel`（solve_tril.py）

**作用**：把 64×64 下三角矩阵 `(I + A)` 求逆，分 4 个 16×16 对角块各自迭代逆传播，再用跨块 dot 串起来。

**优化点**（提交 `8508c6f`）：
1. **autotune 收敛到单点** `num_warps=8, num_stages=5`，从 12 组 config 砍到 1 组，消除 JIT 首包损耗。
2. **删 `tl.where(m_A, ...)` 空 pass**：上游已保证 b_A 对角和上三角为 0，直接 `-b_Ai_xx * b_tril` 替代 4 次 16×16 vec 操作。
3. **Python 端预计算 tril/eye mask 缓冲**，避免 kernel 内 tl.arange 比较和 tl.where。
4. **4 个独立 for 合并成 1 个**：原本 4 个 block 各自 `for i in 2..16` / `for i in 18..32` ...，合并后共享 i ∈ [2, 16) 单一迭代变量，4 块在每一轮内并行调度，循环计数器开销摊到 1×。

**关键指标**：

| 指标 | 8K BASE | 8K NEW | 1K BASE | 1K NEW |
|---|---:|---:|---:|---:|
| Task Duration | 6,429 us | **4,815 us** | 817 us | **615 us** |
| 收益 | - | **-25.1%** | - | **-24.7%** |

注：曾尝试 H_PACK=8 但 +3.4% 劣化（merge 内部 14 轮 row-update 串行依赖重，H_PACK 后单 program 串行链长度 ×8 抵消启动税收益），已回退。

---

### 6. `recompute_w_u_fwd_kernel`（kda.py）

**作用**：用上游求好的 `A`、`β`、`gk` 重算 `w = A·(β·K·exp(gk))`、`u = A·(β·V)`，可选输出 `kg = K·exp(g_last - gk)`。

**优化点**（提交 `d311d63`）：
1. **BK/BV 拍平到 `next_power_of_2(K)/next_power_of_2(V) = 128`**：原 BK=BV=64 在 K=V=128 下 i_k/i_v 各走 2 步，拍平后塌缩为单步，干掉一半 `make_block_ptr`、`boundary_check`、scalar 控制流。
2. **H_PACK=8**：grid 第二维从 `B*H=32` 降到 `B*(H/H_PACK)=4`，Block Dim 4096 → 512，wave 数 ~171 → ~21。bos/last_idx 不依赖 head 在展开块外只算一次。
3. **删除 `STORE_QG` 死代码**：caller 始终传 `qg=None`，相关分支永远走不到，整段以及形参 `q`、`qg` 一并删除。

**关键指标**：

| 指标 | 8K BASE | 8K NEW | 1K BASE | 1K NEW |
|---|---:|---:|---:|---:|
| Task Duration | 3,150 us | **1,388 us** | 433 us | **222 us** |
| 收益 | - | **-55.9%** | - | **-48.6%** |
| Block Dim | 4,096 | 512 | - | - |
| aic_scalar_ratio | 17.5% | 1.2% | - | - |
| aic_mac_ratio | 1.6% | 3.2% | - | - |
| cube_utilization | 97.2% | 97.8% | - | - |

---

### 7. `chunk_gated_delta_rule_fwd_kernel_h_blockdim64`（chunk_delta_h.py）

**作用**：沿 T 方向递推 KV 状态 `h`，每个 (i_t, i_b, i_h) program 顺序处理 NT 个 chunk，dotloop 写出 `(B, NT, H, V, K)` 的 `h`。

**优化点**（提交 `e906fc6`）：
1. **autotune 收敛 `BV=128, num_warps=8, num_stages=3`**：原 sweep 网格 `BV ∈ {32,64} × num_warps ∈ {2,4} × num_stages ∈ {2,3,4}` 共 12 组 config，且 BV 最大 64 在 V=128 下 i_v 维 = 2，每个 (i_t, i_b, i_h) 在 V 方向上要跑两个 program。固化 BV=128 让 `cdiv(V, BV) = 1`，i_v 塌缩为单步，program 数减半；num_warps=8 提升 dot 流水的计算密度。

**关键指标**：

| 指标 | 8K BASE | 8K NEW | 1K BASE | 1K NEW |
|---|---:|---:|---:|---:|
| Task Duration | 3,809 us | **2,170 us** | 491 us | **274 us** |
| 收益 | - | **-43.0%** | - | **-44.1%** |
| aiv_vec_ratio | 20.6% | 44.2% | - | - |
| aic_scalar_ratio | 48.6% | 37.6% | - | - |

---

### 8. `l2norm_fwd_kernel2`（l2norm.py）

**作用**：对 `q/k` 张量做行 L2 归一化，input shape `(T', D) = (262144, 128)`。在 chunk_kda 流水里被调用 4 次（kernel 内 + 测试 reference 内 q/k 各 1 次）。

**优化点**（提交 `128326c`）：
1. **MBLOCK 从 32 提到 128**：单 program 处理 4× 行数，case T'=262144 时 grid 8192 → 2048，wave 数 ~342 → ~86。UB 用量 128×128×4(fp32) = 64KB，远低于 910B UB 上限 192KB。
2. **`BD == N` 时去掉多余 `tl.where(xmask, square, 0)`**：D=128 是常见路径，BD=next_pow2(128)=128，行 mask 在 `tl.load` 时已生效，再过一次 `tl.where` 是空 pass。

**关键指标**：

| 指标 | 8K BASE | 8K NEW | 1K BASE | 1K NEW |
|---|---:|---:|---:|---:|
| Task Duration（4 次累计） | 1,227 us | **579 us** | 159 us | **79 us** |
| 单次 Task Duration | 318 us | 145 us | 40 us | 20 us |
| 收益 | - | **-52.8%** | - | **-50.4%** |
| Block Dim | 8,192 | 2,048 | - | - |
| aiv_scalar_ratio | 18.6% | 7.3% | - | - |

## 三、流水内瓶颈分布对比

### NEW 状态下各 kernel 占流水比例

| kernel | 8K us | 8K % | 1K us | 1K % |
|---|---:|---:|---:|---:|
| intra_sub_inter | 6,160 | **30.2%** | 899 | **32.4%** |
| merge_16x16 | 4,815 | 23.6% | 615 | 22.1% |
| intra_sub_intra | 4,353 | 21.4% | 578 | 20.8% |
| gated_delta_rule_h | 2,170 | 10.6% | 275 | 9.9% |
| recompute_w_u | 1,388 | 6.8% | 222 | 8.0% |
| l2norm | 579 | 2.8% | 79 | 2.8% |
| chunk_gla_fwd_o | 521 | 2.6% | 57 | 2.1% |
| cumsum | 398 | 2.0% | 54 | 1.9% |

两个 case 的瓶颈排序完全一致，且每个 kernel 的占比相差不超过 1 个百分点。

## 四、优化手法总结

不同 kernel 用到的核心套路高度可复用，归纳为以下五类：

| 手法 | 应用 kernel | 核心目的 |
|---|---|---|
| **H_PACK=8 静态展开** | cumsum、chunk_gla_fwd_o、intra_sub_intra、recompute_w_u、l2norm（MBLOCK） | 一个 program 处理多个 head/行块，wave 数 ÷8 摊薄启动税（~62 us/wave） |
| **autotune 收敛单点** | chunk_gla_fwd_o、merge_16x16、gated_delta_rule_h、recompute_w_u、intra_sub_inter | 干掉 JIT 首包损耗与 autotuner 噪声选错 |
| **BK/BV 拍平到 next_pow2(K/V)** | intra_sub_inter、recompute_w_u、chunk_gla_fwd_o | i_k/i_v 循环塌缩为单步，删一半 make_block_ptr 与 boundary_check |
| **`tl.dot` 替代 vec reduce** | intra_sub_intra（最关键） | 走 cube 单元，cube 利用率从 0% 提到 96% |
| **载入 hoist + 死代码 / 冗余 mask 删除** | intra_sub_inter（i_i hoist）、merge_16x16（删 tl.where 空 pass）、l2norm（删 BD==N 时 tl.where）、recompute_w_u（删 STORE_QG） | 减少重复 mte2 与 vec pass |

启动税 (~62 us/wave) 在 NPU triton 上是一个反复出现的瓶颈，多数 kernel 通过 H_PACK 把 wave 数压到 24 以下后就能拿到大头收益。

## 五、未优化成功的尝试（已回退）

1. **intra_sub_inter 优化 3：i_j-only 预载 b_kt/b_gk**（dict 形式）：触发 NPU **aicore timeout（错误码 507014）**，整个 kernel 跑 9 分半钟未完成。原因推测是 Triton-Ascend lowering 把 `if i_j == 0/1/2` 处理成大量条件 mask，加上同时驻留 UB 的 `b_kt0/1/2` 触发 register spill。
2. **merge_16x16 H_PACK=8**：+3.4% 劣化。原因是 merge 内部 14 轮 row-update（`for i: tl.where(cond, b_a, b_Ai)`）有重串行依赖，H_PACK=8 后单 program 串行链长度 ×8，cube 闲置时间增加抵消启动税收益。

## 六、未来可继续优化的方向

按 ROI 排序：

| kernel | 当前耗时（8K） | 占流水 | 已优化深度 | 备注 |
|---|---:|---:|---|---|
| intra_sub_inter | 6,160 us | 30.2% | 中（aiv_scalar 仍 70%） | 当前流水头号瓶颈，可继续找算法层面新角度 |
| merge_16x16 | 4,815 us | 23.6% | 浅（H_PACK 失败） | 行 update 模式难拆，需要算法重构 |
| intra_sub_intra | 4,353 us | 21.4% | 深（cube 利用 96%） | 已接近极限，再挖空间小 |
| gated_delta_rule_h | 2,170 us | 10.6% | 浅（仅 autotune） | 可尝试 dot 路径重构或 H_PACK |
| recompute_w_u | 1,388 us | 6.8% | 深（H_PACK + 拍平） | 接近极限 |
| l2norm | 579 us | 2.8% | 中（MBLOCK 拉大） | 绝对值小 |
| chunk_gla_fwd_o | 521 us | 2.6% | 深 | 已 -96% |
| cumsum | 398 us | 2.0% | 深 | 已 -99% |
